Windows Forms application that allows the user to load an external list of students and their test scores, which may be altered inside the application. Resulting information is saved back into the external text file.